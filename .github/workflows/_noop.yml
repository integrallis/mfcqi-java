name: _noop
# Throwaway target used to verify PACKAGES_PUBLISH_TOKEN can dispatch workflows. Safe to delete.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "noop ran — the dispatching token has Actions: write on this repo."
